<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title><%= title %></title>
</head>
<body>
  <ul>
    <% for(let i = 0; i < messages.length; i++) { %>
      <li>New message received at <% console.log(messages[i].text)%>
        <%= messages[i].added%>:<%= messages[i].text %> by <%= messages[i].username%> <a href="/messages/<%= i%>"> Show Message</a></li>
    <%};%>
  </ul>
</body>
</html>
